package timeReward;

import javax.swing.*;
import java.awt.*;

public class View extends JFrame {
    public View() {
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setLayout(null);
        setSize(200, 100);

        setVisible(true);
    }
}
